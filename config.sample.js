// Copy this file to `config.js` and add your GIPHY API key.
//
//   cp config.sample.js config.js   (then paste your key below)
//
// `config.js` is gitignored so your key is never committed to the repo.
// Get a free key at https://developers.giphy.com/dashboard/ (create an app).
//
// NOTE: a browser extension cannot truly hide an API key — whatever you put
// here ships inside the published extension and can be extracted by anyone.
// Use a key dedicated to this project and keep an eye on its usage limits.
window.TRENGIF_CONFIG = {
  GIPHY_API_KEY: "YOUR_GIPHY_API_KEY",
};

{
  "name": "trengif-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy that keeps the GIPHY API key off the client for the TrenGIF extension.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "license": "MIT"
}
